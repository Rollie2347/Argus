# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Canonical directory

Use `Argus/` (capital A). An older lowercase `argus/` Python/FastAPI prototype may exist on some local machines/sandboxes (it's referenced in `start.sh`, a leftover from an earlier hackathon layout) — it is **not** in the GitHub repo and should not be treated as current.

## What this is

Argus is a real-time multimodal AI life companion built on the Gemini Live API (vision + voice), built for the Gemini Live Agent Challenge. One WebSocket connection = one persistent Gemini Live session with camera frames, mic audio, and tool-calling flowing bidirectionally.

## Repo layout

```
Argus/
├── backend/           # Node/Express/WebSocket relay to Gemini Live — the core service
│   ├── server.js        # Entry point, port 8080, path /ws
│   ├── agents.js        # Tool declarations + handlers + system prompt (14 tools)
│   ├── memory.js        # Firestore persistence
│   └── weather.js       # Open-Meteo integration
├── frontend/index.html  # Vanilla JS PWA — most complete camera/audio path, use this to validate changes
├── frontend/privacy.html # Privacy policy, served at GET /privacy — required for App Store Connect
├── mobile/             # Expo/React Native app — newer surface, not feature-complete (see below)
├── terraform/main.tf   # IaC for Cloud Run + Firestore
├── Dockerfile
├── deploy-cloudrun.sh
├── ARCHITECTURE.md      # Diagrammed system overview + data flow
└── README.md
```

## Commands

No lint is configured anywhere in this repo, and mobile still has no test suite (no CI beyond the Claude Code review/assistant workflows in `.github/workflows/`). `backend/` has a minimal smoke suite added 2026-07-12 — see `npm test` below. For everything beyond that, verify changes by running the backend and exercising the WebSocket flow through `frontend/index.html`.

**Backend**
```bash
cd backend
npm install
cp .env.example .env   # set GEMINI_API_KEY, GCP_PROJECT_ID at minimum
npm start               # node server.js — http://localhost:8080
npm run dev              # node --watch server.js — auto-restart on change
npm test                 # node --test "test/**/*.test.js" — smoke tests, see below
cloudflared tunnel --url http://localhost:8080   # HTTPS tunnel, required for phone camera/mic testing
```
Local dev also needs a Firestore `backend/service-account.json` (gitignored). Cloud Run uses default credentials instead.

`npm test` runs `backend/test/server.smoke.test.js`: spawns the server with a throwaway `GEMINI_API_KEY`/`WS_SHARED_SECRET`/no real Firestore creds, then checks `GET /api/health` and the WS auth gate (missing secret / wrong secret / correct secret, asserting the 4001 close behavior). It does **not** exercise a real Gemini Live round trip — that needs a real API key and would cost money on every test run, so it's deliberately out of scope. Run a single file directly with `node --test test/server.smoke.test.js` if needed; on this checkout, `node --test test/` (directory form, no glob) failed to resolve on Windows/Git Bash — use the quoted glob form from the npm script instead.

**Mobile (Expo)**
```bash
cd mobile
npm install
npm start        # expo start
npm run android  # expo run:android
npm run ios       # expo run:ios
```

**Deploy**
```bash
./deploy-cloudrun.sh YOUR_GEMINI_API_KEY YOUR_WS_SHARED_SECRET [LAT LON TIMEZONE MAX_GLOBAL_CONCURRENT_SESSIONS]
```
Hardcoded project `agus-488919`; enables Cloud Run/Artifact Registry/Cloud Build/Firestore and deploys with session affinity (required — WebSocket connections must stick to one instance). `WS_SHARED_SECRET` gates the WebSocket endpoint (see Security below) and is a required startup var, same as `GEMINI_API_KEY` — the server exits immediately if either is missing. As of the 2026-08-09 scaling audit (known issue #32), the deploy sets `--memory 1Gi --concurrency 40 --max-instances 15` (was `512Mi` / implicit concurrency 80 / `10`) plus `MAX_GLOBAL_CONCURRENT_SESSIONS` (default 250 — a fleet-wide connection cap, separate from the per-IP one). Terraform alternative in `terraform/main.tf` (`terraform init && terraform apply -var="gemini_api_key=..." -var="ws_shared_secret=..." -var="project_id=..."`) — also has an optional `google_billing_budget` resource, gated on `-var="billing_account_id=..."`.

## Architecture

- **Backend is a stateful relay, not a REST API.** `server.js` upgrades each `/ws` connection to a WebSocket, opens one Gemini Live session per connection, and pipes camera JPEGs (`image/jpeg`) and mic audio (`audio/pcm;rate=16000`) to Gemini, then streams audio/text/tool-call responses back. Session lifetime = connection lifetime.
- **Tools are in-process functions, not microservices.** `backend/agents.js` defines 14 Gemini function declarations (`identify_scene`, `get_recipe_suggestion`, `cooking_timer`, `compare_products`, `diagnose_problem`, `read_text`, `manage_shopping_list`, `remember_preference`, `recall_memory`, `get_weather`, `log_daily_activity`, `get_daily_summary`, `web_search`, `get_restaurant_website`) plus their handlers and the system prompt (casts Argus as proactive, warm, concise — 1-3 sentence voice replies). When Gemini calls a tool, `server.js` executes the local handler and feeds the result back into the same session.
- **Memory is Firestore, keyed by user.** `backend/memory.js`: `users/{userId}` profile, `users/{userId}/daily/{date}` logs, `users/{userId}/lists/shopping`, `users/{userId}/observations/*`.
- **Every WebSocket connection must present `WS_SHARED_SECRET` before anything else happens.** Fixed 2026-07-11: the client's first `{type:"user_id"}` message must include a `secret` field matching the server's `WS_SHARED_SECRET` env var (`crypto.timingSafeEqual` comparison in `server.js`); a missing/wrong secret closes the socket (code `4001`) before any geo lookup or Gemini session is opened. This was added because the repo is public and `mobile/services/websocket.ts` had hardcoded the live Cloud Run URL as a fallback default, meaning the endpoint was effectively already public with zero validation. `frontend/index.html` gets the secret injected server-side at request time (never committed); mobile reads it from `EXPO_PUBLIC_WS_SHARED_SECRET`. `server.js` also enforces per-IP HTTP rate limiting on `/api/*`, a per-IP WebSocket connection cap, per-connection message-rate limiting, and message type/size validation — all in-memory (resets per Cloud Run instance, doesn't coordinate across concurrent instances; acceptable at personal-testing scale). **Beyond that gate, individual tool calls and memory reads/writes still trust the self-asserted `userId` alone — except destructive HTTP calls, which require a separate per-user device secret.** `POST /api/user/:userId/claim` mints a one-time 256-bit secret (`crypto.randomBytes`) the first time a given `userId`'s Firestore doc is created; the claim transaction refuses (`409`) if the doc already exists for any reason, so an in-use id can never be re-claimed by a different caller. `DELETE /api/user/:userId` (used by the mobile "Delete my data" control) requires that secret as `Authorization: Bearer <secret>`, verified via `verifyDeviceSecret` in `memory.js` with a timing-safe comparison; it 403s otherwise. Mobile claims the secret once in `saveUser()` (`mobile/services/auth.ts`) and stores it in Keychain via `expo-secure-store`. `GET /privacy` serves `frontend/privacy.html` (required for App Store Connect).
- **Weather is unauthenticated.** `backend/weather.js` calls Open-Meteo directly (no API key), caching results in a `Map` keyed by rounded coordinates (`lat.toFixed(2),lon.toFixed(2)`) for 30 minutes per location.
- **`frontend/index.html`** is the reference client: captures JPEG frames every ~2s, streams PCM16 mic audio, plays back PCM24 responses sequentially, and is the fastest way to confirm the Gemini Live pipeline still works end-to-end.
- **`mobile/`** connects via `services/websocket.ts` (`ArgusSocket`), streams mic audio, captures camera frames every ~2s (`startFrameLoop` in `home.tsx`), and plays back incoming PCM audio via a queued `Audio.Sound` (WAV-wrapped, since expo-av can't play raw PCM buffers directly).

## Verified known issues (prioritized)

1. ~~Duplicate `@google-cloud/firestore` in `backend/package.json`~~ — **Fixed 2026-07-11:** removed the dead `^8.3.0` line; `^7.0.0` (already what `package-lock.json` resolved to) is now the only entry.
2. ~~`currentUserId` is a module-level global in `agents.js`~~ — **Fixed 2026-07-11:** removed `currentUserId`/`setUserId()`; `handleToolCall(functionCall, userId)` and `buildSystemInstruction(lat, lon, city, userId)` now take `userId` explicitly, threaded from a per-connection closure variable in `server.js`. This also surfaced and fixed a deeper bug: `buildSystemInstruction` was previously called *before* the client's `user_id` message could ever be received (the message listener wasn't registered yet at that point), so every connection's initial memory context was built from a stale/default id, not just under concurrent load. `server.js` now runs `waitForUserId()` (listens immediately on connect, 3s timeout fallback to `"default"`) before building the system instruction or opening the Gemini session.
3. ~~Weather cache not keyed by location~~ — **Fixed 2026-07-11:** `backend/weather.js` now uses a `Map` keyed by rounded lat/lon instead of one shared cache slot.
4. **Mobile (`mobile/app/(main)/home.tsx`) — frame capture and audio playback fixed 2026-07-11:**
   - ~~Frame capture~~: `home.tsx` now runs a 2s `setInterval` (`startFrameLoop`/`stopFrameLoop`) that calls `cameraRef.current.takePictureAsync({ base64: true, quality: 0.5, ... })` and forwards frames via `socketRef.current.sendImage(...)`. Note: unlike the web client (which downsamples to a 640×480 canvas), mobile frames are only JPEG-compressed (`quality: 0.5`), not resized — expo-camera has no built-in resize, so mobile frames are larger over the wire. Not addressed; flag if bandwidth becomes an issue.
   - ~~Audio playback~~: incoming `"audio"` messages are now queued (`enqueueAudio`/`playNextInQueue`) and played sequentially via `Audio.Sound`, each PCM16/24kHz chunk wrapped in a WAV header (`pcmToWavBase64`) since expo-av can only load audio files/URIs, not raw buffers.
   - Net effect: the Expo app should now have working vision input and voice output, matching the web PWA's capabilities, *pending* a working app launch — see known issue #15, found 2026-07-13 during the first physical-device test: the app crashed to expo-router's "Unmatched Route" screen before camera/mic could even be exercised. Still genuinely untested: iOS audio-session interaction between simultaneous mic recording (`allowsRecordingIOS: true`) and playback, which may need `interruptionModeIOS`/`playThroughEarpieceAndroid` tuning if audio doesn't play through cleanly during a live conversation — blocked on #15 first.
5. **Model retirement risk.** `server.js` currently targets a Gemini Live preview model — preview model names get retired without much notice. Check current availability before assuming the configured model still works, and re-verify the model string in `server.js` against the Gemini API docs periodically.
6. ~~`mobile/.env` was previously committed~~ — **corrected 2026-07-11:** checked full non-shallow history (`git log --all --diff-filter=A --name-only`) across every ref; only `mobile/.env.example` was ever added under `mobile/`. The commit titled "Stop tracking mobile/.env" only touches `.gitignore`, not a tracked file. No secret was ever committed here and none is rotation-needed. `mobile/.env` is gitignored alongside `.env`, `service-account.json`, and `node_modules/` as a preventive measure — keep it that way, but there's no cleanup action pending.
7. ~~Mobile dependency versions needed real SDK 54 alignment~~ — **Fixed 2026-07-11, `npm install` completed and verified 2026-07-12:** `mobile/package.json` had declared `expo: ~54.0.0` / `app.json` had `sdkVersion: 54.0.0` while every sibling native module and `package-lock.json` were still fully on a consistent SDK 52 tree — an inconsistent half-migrated state that would very likely have broken `eas build`. Fixed by bumping every Expo-bundled native module to the versions pinned in `expo@54.0.0`'s `bundledNativeModules.json`. `npm install` has since actually been run (`mobile/node_modules` exists) and `npx expo-doctor` passes 18/18 checks; the install also surfaced two missing `expo-router` peer deps (`expo-constants`, `expo-linking`), now added to `package.json`. **Still not verified on a real device/simulator** — that's the one thing that requires your physical iPhone, see Session status below.
8. ~~Unused `@google/adk` dependency and never-installed `backend/node_modules`~~ — **Fixed 2026-07-12:** `@google/adk` was never imported anywhere (only referenced in a `server.js` comment) but pulled in the entire ADK dependency tree (`@mikro-orm/*`, `hono`, `sqlite3`, `mariadb`, etc.), accounting for 41 of 58 `npm audit` findings including both criticals. Removed it; `backend/node_modules` had also never been installed in this checkout — ran `npm install`, confirmed `node server.js` resolves all imports and fails cleanly only on the expected missing-`GEMINI_API_KEY` check. Backend audit findings now 6 moderate (deep transitive `uuid`/`gaxios` deps of `@google-cloud/firestore`, no fix without a breaking major bump — not worth forcing). Also fixed: root `.gitignore`'s `mobile/.env` line had been appended as UTF-16LE at some point (likely a PowerShell append with default encoding) and never actually matched anything — rewrote the file as plain UTF-8. No behavior change in practice since `mobile/.gitignore` already had its own working `.env` entry covering the same path.
9. ~~WebSocket endpoint had zero auth, rate limiting, or input validation~~ — **Fixed 2026-07-12, see the Architecture section above for the mechanism.** This was the highest-severity finding of the session: the repo is public and the live Cloud Run WS URL was hardcoded in `mobile/services/websocket.ts` as a fallback default, so the unauthenticated endpoint was already effectively public. Verified live with a throwaway local secret: unauthenticated/wrong-secret WS connections get closed (code `4001`) before any Gemini session opens; correctly-authenticated ones proceed normally.
10. ~~`deploy/` (`deploy/deploy.sh`, `deploy/main.tf`) was a stale duplicate that predated the `WS_SHARED_SECRET` work~~ — **Removed 2026-07-12.** It diffed against `deploy-cloudrun.sh`/`terraform/main.tf` as missing the `ws_shared_secret` variable entirely and using an older argument order (`PROJECT_ID API_KEY`, no secret) — deploying through it would have stood up the Cloud Run service with the WS endpoint unauthenticated again. Confirmed unreferenced anywhere else in the repo (no CI, no README, no other doc) before deleting. Use `deploy-cloudrun.sh` or `terraform/main.tf` for all deploys.
11. ~~`mobile/app.json`'s Android `permissions` array requested `READ_SMS` and `RECEIVE_SMS`~~ — **Fixed 2026-07-12:** confirmed zero SMS-related code anywhere in `mobile/` (`mobile/services/gmail.ts` is an empty two-line stub, `// Gmail integration — coming in v1.1`, and nothing else references SMS), so removed both from the `permissions` array. `READ_SMS`/`RECEIVE_SMS` are Google Play restricted permissions requiring a declaration form and justification in Play Console; they were an unnecessary Play Store rejection risk and privacy footprint. `permissions` is now just `["CAMERA", "RECORD_AUDIO"]`, matching what's actually used.
12. **The `WS_SHARED_SECRET` fixed in known issue #9 is served in plaintext to anyone who loads the public site — it does not actually gate access the way the name implies.** `server.js`'s `serveIndexWithSecret` (the handler for `GET /` and `/index.html`) injects the live `WS_SHARED_SECRET` value directly into the HTML sent to *any* visitor, no login or check required. View-source (or `curl` the URL) trivially recovers the secret. This means the auth gate only stops connections that never touch the frontend page at all (blind scripted hits directly against `/ws`, which was the literal threat described in #9) — it does **not** stop anyone who loads the public URL once, extracts the secret, and then scripts unlimited direct WS connections from unlimited IPs (the per-IP cap of 5 connections in `server.js` is easy to route around with multiple IPs/proxies, and there is no global concurrency cap at all). Practically this bounds cost only against *unaware* abuse, not *deliberate* abuse by anyone who visits the page — worth deciding whether that's an acceptable risk at current (personal-testing / hackathon-demo) scale, or whether the frontend needs a different distribution model (e.g. a short-lived per-session token minted by an authenticated endpoint instead of a static secret baked into public HTML). **Decision (2026-07-12): accepted as-is.** User wants the web frontend to stay low-traffic/unpromoted and the iOS app to be the surface that gets real traffic — given that, not worth the redesign. Revisit if the web page is ever linked/promoted.
13. ~~EAS iOS builds failed at "Install dependencies" (`npm ci --include=dev`) with an ERESOLVE conflict~~ — **Fixed 2026-07-12.** Root cause: `expo-router@6.0.24` has a genuine (non-peer) dependency on `vaul` (a web-only drawer component, used for expo-router's web output target), which depends on `@radix-ui/react-dialog`, whose peer range on `react-dom` (`^16.8 || ... || ^19.0.0-rc`) resolved to `react-dom@19.2.7` in the lockfile — a version requiring `react@^19.2.7`, conflicting with this project's exactly-pinned `react@19.1.0` (pinned to match Expo SDK 54's bundled native module versions per issue #7). `npm install` tolerated this silently; EAS Build's `npm ci --include=dev` does not, since `npm ci` enforces the lockfile strictly. Fixed by adding `"overrides": {"react-dom": "19.1.0"}` to `mobile/package.json` (matches the pinned `react` version, avoids a blanket `legacy-peer-deps` that would hide other future conflicts) and regenerating `package-lock.json`. Verified locally by actually running `npm ci --include=dev` (not just `npm install`) after a full `node_modules` wipe — succeeds cleanly — plus `npx expo-doctor` (18/18 passed).
14. ~~EAS iOS build failed at "Bundle JavaScript" with `Cannot find module 'babel-preset-expo'`~~ — **Fixed 2026-07-13.** `babel.config.js` does `presets: ["babel-preset-expo"]`, resolved via Node's module resolution from the project root — but `babel-preset-expo` was never a direct dependency in `mobile/package.json`, only a nested dependency of `expo` itself (`node_modules/expo/node_modules/babel-preset-expo`), which a root-level `require()` can't see. Every standard Expo template declares this directly as a devDependency for exactly this reason; this repo's hand-edited SDK 52→54 migration (known issue #7) missed it. Fixed by adding `"babel-preset-expo": "~54.0.11"` (matching the version `expo@54.0.35` itself depends on) to `mobile/package.json`'s devDependencies. Verified by reproducing the exact failing EAS step locally: `npx expo export:embed --eager --platform ios --dev false` now bundles successfully (1003 modules), after a fresh `npm ci --include=dev`.
15. ~~First physical-device test (TestFlight, production build, 2026-07-13) crashed to expo-router's "Unmatched Route" screen~~ — **Fixed 2026-07-16.** Confirmed root cause: there was no `app/index.tsx` anywhere in the project, and `mobile/app/_layout.tsx` rendered an empty `<Stack />` while doing the auth redirect *imperatively* inside a `useEffect` (`router.replace("/(main)/home")` or `router.replace("/sign-in")`). With no file matching route `/` at all, the router had nothing to land on at initial render in a production Hermes bundle — a documented expo-router footgun — and the async `useEffect` redirect raced the navigator's mount instead of resolving it. Fixed by adding `mobile/app/index.tsx`: it renders a loading spinner while `getStoredUser()` resolves, then a declarative `<Redirect href={...} />` to `/sign-in` or `/(main)/home`. `_layout.tsx` is now just `<StatusBar />` + `<Stack screenOptions={...} />`, no logic. Verified via `npx tsc --noEmit` (clean) and by reproducing EAS Build's exact "Bundle JavaScript" step locally (`npx expo export:embed --eager --platform ios --dev false`) — bundles successfully with the new route included. Independently re-verified by a second review pass reading the changed files fresh, then **confirmed fixed on a real device**: rebuilt, resubmitted, installed via TestFlight — app now launches past the auth check with no crash.
16. ~~`remember_preference` tool handler silently dropped non-dietary/allergy/name preferences~~ — **Fixed 2026-07-16.** `backend/agents.js`'s `remember_preference` case built an update key like `` `preferences.${args.key}` `` and passed it to `updateUserMemory` → `db.collection("users").doc(userId).set(data, {merge:true})` in `backend/memory.js`. Confirmed by reading `@google-cloud/firestore`'s SDK source (`document.js`'s `DocumentMask.fromObject`, comment: *"We don't split on dots since fromObject is called with DocumentData"*) that `set(..., {merge:true})` does **not** parse dotted top-level keys as nested field paths the way `update()` does — so this was writing a literal field named `preferences.foo` (dot included in the field name) instead of nesting under a `preferences` map. `buildMemoryContext`'s `if (userMem.preferences) ...` check in `memory.js` never saw it, so any preference stored under category `favorite`/`dislike`/`routine`/`other` was silently never recalled in later sessions (only `dietary`/`allergy`/`personal`+`name`, which write real top-level fields, worked). Fixed by reading the existing `preferences` map first and merging the new key into a real nested JS object before writing: `update.preferences = { ...(current.preferences || {}), [args.key]: args.value }`. Known minor residual risk, not fixed (low severity, flagged in review): this read-then-write isn't transactional, so two `remember_preference` calls for different keys arriving close enough together (two overlapping `toolCall` messages) could race and one write could clobber the other. Not worth hardening at current single-user/personal-testing scale — would be `update({[\`preferences.${key}\`]: value})` (atomic, no read) if it ever needs fixing.
17. ~~`production` EAS submissions were blocked: first by the placeholder `EXPO_PUBLIC_BACKEND_URL`, then by an Apple build-number collision~~ — **Fixed 2026-07-16.** After #15 was fixed and rebuilt, `eas submit` failed with a generic "Something went wrong" — the CLI truncates the real reason; `eas submit --platform ios --latest --verbose-fastlane` surfaced it: *"Build number 1 for app version 1.0.0 has already been used."* `mobile/eas.json` had `appVersionSource: "remote"` but no `autoIncrement` on the build profile, so every EAS build reused build number 1, and the very first submission attempt had already registered that number with Apple before failing at a later step. Fixed by adding `"autoIncrement": true` to the `production` build profile in `eas.json`, so each new build gets a fresh number. Also added `"submit": {"production": {"ios": {"ascAppId": "6761696821"}}}` to skip the interactive "ensure app exists on App Store Connect" step (the ASC app ID isn't a secret). Separately, confirmed via `eas env:list --environment production` that `EXPO_PUBLIC_BACKEND_URL` was still the literal placeholder `https://<your-cloud-run-url>` from known issue #12's earlier deferral — updated it to the real live URL (`https://argus-798059802495.us-central1.run.app`, verified reachable via `/api/health` before wiring it in) with `eas env:update production --variable-name EXPO_PUBLIC_BACKEND_URL --value ...`. Rebuilt with both fixes, resubmitted, installed on-device — connects and gets a spoken response successfully for the first time.
18. ~~Argus's spoken responses were jittery / breaking up on-device~~ — **Fixed 2026-07-16.** `mobile/app/(main)/home.tsx`'s `playNextInQueue` created a brand-new `Audio.Sound` for every individual incoming PCM chunk from Gemini's audio stream (which arrives in many small pieces), and only started loading the *next* chunk's `Sound` after the current one's `didJustFinish` fired — so every chunk boundary incurred a real native load/decode gap, which is what read as jitter/breakup. `expo-av` can't append raw PCM to an already-playing sound, so chunk-per-chunk playback was never going to be gapless. Fixed by draining and merging whatever chunks accumulated in `audioQueueRef` into a single WAV buffer per playback burst (renamed `pcmToWavBase64` → `pcmChunksToWavBase64`, now takes an array) instead of wrapping and playing one chunk at a time — cuts the number of `Sound`-object load boundaries from roughly one per chunk down to roughly one per burst. Verified via `tsc --noEmit` and a local reproduction of EAS's bundling step, then **confirmed on a real device** — rebuilt, resubmitted, playback no longer jitters.
19. ~~Home screen was mostly empty black space~~ — **Fixed 2026-07-16.** `mobile/app/(main)/home.tsx`'s camera view was capped at `maxHeight:260` while the transcript `ScrollView` below it was `flex:1` — with only a few lines of transcript, that flex area was mostly empty on a modern iPhone screen. Redesigned: camera (or the dormant placeholder) is now `flex:1` and fills essentially the whole content area between the header and the bottom controls; the status badge floats as a small pill over the top of the camera instead of taking its own row; the transcript is now an absolutely-positioned semi-transparent overlay anchored to the bottom of the camera view (`maxHeight:"45%"`, only rendered once `lines.length > 0`) instead of a permanent full-height panel. Verified via `tsc --noEmit` and a local bundle reproduction, then **confirmed on a real device** as part of the same rebuild as #18.
20. ~~Several TestFlight-tester UI complaints: confusing mute button, silent long waits read as "broken," stacking error messages, emoji tool-status text, quiet playback~~ — **Fixed 2026-07-28 (code-level; not yet rebuilt/resubmitted to TestFlight as of this writing).** All in `mobile/app/(main)/home.tsx`:
    - **Mute control**: the mic button was a bare icon (`🎙️`/`🔇`) that testers found unclear. Replaced with a standard `Switch` (on = mic active) plus a persistent "Mic" label. (A first pass also flashed a "Muted"/"Unmuted" label on toggle that faded out after ~1.4s; removed per user feedback — just the slider, no flash text.)
    - **Long waits reading as broken**: added a `lastActivityRef` timestamp updated on every incoming socket message, checked once a second while `status==="observing"`. After 3.5s idle the status badge swaps to "Still thinking...", after 8s to "Taking a bit longer than usual, still here...", clearing the instant any new message arrives.
    - **Stacking error messages**: connection errors used to go through `addLine` into the permanent transcript (`msg.type==="error"` case), so repeated errors piled up and never left the screen. Now routed to a separate `errors` state rendered via a new `ErrorToast` component — each toast fades in, holds ~3.8s, fades out, and self-removes; capped at 3 concurrent toasts.
    - **Emoji tool-status text**: the `tool_event` line rendered `"⚙ " + msg.tool` (e.g. "⚙ read_text"). Added a `TOOL_LABELS` map translating all 14 backend tool names (`backend/agents.js`) to plain-English text with no prefix (e.g. "Reading the text", "Looking at what's around you").
    - **Playback too quiet even at max volume**: read `expo-av`'s native iOS source (`node_modules/expo-av/ios/EXAV/EXAV.m`) and confirmed it never requests `AVAudioSessionCategoryOptionDefaultToSpeaker` — with `allowsRecordingIOS:true` (required for the simultaneous mic+playback in known issue #4), iOS's `.playAndRecord` category defaults output to the quiet earpiece receiver, not the main speaker, and expo-av's JS API exposes no way to override that routing. That was the root cause — actually patched in known issue #21 below (this session deferred the native patch; the next one did it). As a same-session JS-level mitigation, `pcmChunksToWavBase64` now applies a 1.6x digital gain to every PCM16 sample (clamped to int16 range to avoid overflow/distortion) before wrapping it as WAV — **may need re-tuning once #21's speaker-routing patch is verified on-device, since the baseline loudness will differ from the earpiece route this gain was tuned for.**
    - Verified via `npx tsc --noEmit` (clean) and reproducing EAS's exact bundling step (`npx expo export:embed --eager --platform ios --dev false`, 1004 modules) — **not yet tested on a physical device**, since no new build/TestFlight submission has happened since this changed. Next session (or later this one) should build + `eas submit` and get real-device confirmation before treating this as done the way issues #15/#18/#19 are.
21. **Hardware volume buttons had no effect on Argus's playback during a session** — **Root-caused and patched 2026-07-28, pending EAS rebuild + physical-device verification.** Same underlying gap as known issue #20's "too quiet" finding, but confirmed to be the cause of a second, more severe symptom: `EXAV.m`'s `_updateAudioSessionCategoryForAudioSessionMode:` (~line 307) selects `AVAudioSessionCategoryPlayAndRecord` whenever `allowsRecordingIOS:true`, and the only category option ever added for that case (~line 328) was `AVAudioSessionCategoryOptionAllowBluetooth` — `DefaultToSpeaker` is never requested anywhere in the library. Without it, output defaults to the built-in earpiece receiver, and iOS ties hardware volume-button behavior to the classification of the active route: receiver-routed `.playAndRecord` audio (the same route an active phone call uses) isn't treated as a normal "media" route, so the physical buttons don't audibly affect it. Fixed via `patch-package`: added `mobile/patches/expo-av+16.0.8.patch`, OR'ing in `AVAudioSessionCategoryOptionDefaultToSpeaker` alongside the existing `AllowBluetooth` option in `EXAV.m` — this only changes the *default* route when no Bluetooth/wired accessory is connected, so headset/Bluetooth routing is unaffected. `mobile/package.json` got `patch-package` as a devDependency and a `postinstall: patch-package` script so the patch reapplies automatically on every fresh `npm install` (verified locally by deleting `node_modules/expo-av` and reinstalling). Verified via `npx tsc --noEmit`, `npx expo-doctor` (patch doesn't break dependency validation), and reproducing EAS's exact bundling step (`npx expo export:embed --eager --platform ios --dev false`) — **not yet tested on a physical device**, since this is a native-code change requiring an EAS rebuild. Next build should confirm (a) hardware volume buttons now work, and (b) whether known issue #20's 1.6x `AUDIO_GAIN` still sounds right now that output routes to the speaker instead of the earpiece (adjust if too loud/clipped).
22. ~~Camera flip + settings tab added; closed-captions toggle and settings-button placement confirmed still broken on-device~~ — **closed captions fixed 2026-07-31, settings-button placement confirmed already correct by the user (was a stale-build report, not a real bug).** All in `mobile/app/(main)/home.tsx` unless noted:
    - Added a front/back camera flip: `facing` state wired into `<CameraView facing={facing}/>`, plus a flip button (`⟲`) in the main controls row next to the mic switch, shown only while connected.
    - Added `mobile/app/(main)/settings.tsx` (new screen, reached via a `⚙` icon in the header) holding a closed-captions toggle (`mobile/services/settings.ts`, `AsyncStorage`-backed, default on). The transcript overlay only renders when captions are enabled.
    - Reworked `tool_event` handling so tool-status text (e.g. "Looking at what's around you") is a transient hint (`showToolStatus`/`clearToolStatus`, auto-clears after 4s or on the next turn) instead of a permanent line stacking up in the transcript alongside real dialogue.
    - First attempt at wiring the captions toggle used `useFocusEffect` on `home.tsx` to re-read `AsyncStorage` every time the screen regained focus from Settings. **User reported this didn't reliably reflect the toggle's state.** Hypothesized cause: `settings.tsx`'s `toggleCaptions` wrote to `AsyncStorage` without awaiting the write, so tapping the switch then immediately tapping "Back" could let Home's focus-triggered read fire before the write landed. Attempted fix: moved to an in-memory cache + pub/sub in `services/settings.ts` (`subscribeCaptionsEnabled`), so `setCaptionsEnabled()` notifies all live subscribers synchronously instead of relying on a storage round-trip; `home.tsx` now subscribes on mount instead of using `useFocusEffect`. Also moved the `⚙` icon from before "Delete my data" to between "Delete my data" and "Sign out", per user request.
    - **Both the captions fix and the button reorder were re-tested on a real device and the user reports neither took effect.** Not yet re-diagnosed — deferred at the user's explicit request ("we will work on them later"). Open questions for whoever picks this back up:
      - Nothing from this entire feature (camera flip, settings tab, both caption-toggle attempts, the header reorder) was committed or pushed until this same session's very next step — confirm the on-device build the user actually tested was built *after* that push, not from an older `git` state or a stale local EAS cache; this repo has prior history (see the `argus-cloudrun-stale-deploy` memory note) of "fixed" code not actually being what was running/tested.
      - If the build genuinely did include this code and it still doesn't work, the pub/sub hypothesis above may be wrong entirely (e.g. a Metro/Fast-Refresh state issue on native, a bundling issue specific to the new `settings.tsx` route, or the header reorder JSX not matching what's actually in the built bundle) — get exact repro detail from the user (screenshot/video of the settings screen and the header) before changing code again, rather than re-guessing.
    - Verified pre-device via `npx tsc --noEmit` (clean) and reproducing EAS's bundling step (`npx expo export:embed --eager --platform ios --dev false`, ~1006 modules, includes the new `settings` route) — these only confirm the code compiles and bundles, not that the fix works, which is exactly the open question above.
    - **Actual root cause and fix, 2026-07-31:** the pub/sub hypothesis above was never conclusively tested against a real device build — turned out to be moot, since the build that shipped to TestFlight/App Review at the time (and the one Apple actually reviewed, build `1.0 (4)` — see known issue #23) predated this entire feature. Rather than keep debugging the hand-rolled `services/settings.ts` singleton, replaced it outright with a `CaptionsProvider` React Context (`mobile/contexts/CaptionsContext.tsx`), mounted once at the app root (`mobile/app/_layout.tsx`). Home and Settings now read/write the exact same React state via `useCaptions()` — eliminates the AsyncStorage-race class of bug by construction instead of patching around it. `mobile/services/settings.ts` deleted. Captions now also default to **off**, with an opt-in step added to `mobile/app/onboarding.tsx` (a 5th slide with a toggle) — previously it defaulted on with no way to decline before first use. Verified via `tsc --noEmit` and the EAS bundling reproduction; **still needs a fresh real-device TestFlight pass** to confirm (bundled into build 16, see known issue #23 — not yet confirmed on-device as of this writing).
    - **Reopened 2026-08-01:** user reports closed captions still don't work, tested against whatever build was installed at the time of the report (build 17 had just been submitted minutes earlier — TestFlight processing normally takes 5-10 minutes, so it's not confirmed the phone was actually running 17 rather than 16 or an even older cached install). **Before writing any new code, confirm which exact build number was on-device when this was tested** — this doc has now recorded two separate "fixed, then reported still broken" cycles for this exact feature (see the first bullet above), and both turned out to hinge on which build was actually running. If it's confirmed build 17 and genuinely still broken, the `CaptionsContext` approach itself needs to be re-examined (e.g. verify the Provider is actually mounted above both `Home` and `Settings` in the tree at runtime, not just in source) rather than assumed correct.

23. **First real App Store review rejection — fixed and resubmitted 2026-08-01, App Store Connect resubmission + real-device TestFlight verification still pending.** Apple's initial notification email only listed guideline numbers/titles (`2.1.0 Performance: App Completeness`, `5.1.1 Legal: Privacy - Data Collection and Storage`) with zero detail — guessed at plausible causes first (missing in-app privacy link, unbounded "Connecting" state) and shipped defensive fixes for those, but the actual Resolution Center message (obtained later, after the guesswork) revealed narrower, different issues:
    - **Guideline 5.1.1(ii):** `mobile/app.json`'s `NSCameraUsageDescription`/`NSMicrophoneUsageDescription` (plus a second, conflicting mic string set via the `expo-av` plugin's `microphonePermission` config) were too vague — e.g. "Argus needs microphone to hear you" is nearly identical to Apple's own listed example of unacceptable purpose strings ("App needs microphone access"). Fixed by rewriting both to state the purpose with a concrete example and disclose that data streams live to Gemini and isn't stored, matching Apple's documented pattern for acceptable strings.
    - **Guideline 2.1 "Information Needed":** not a completeness/crash bug — Apple asked two literal questions in the Resolution Center thread: whether the app sends user data to a 3rd-party AI (and what data), and whether there's paid content. Answered via a Resolution Center reply: camera frames, mic audio, stored preferences/allergies, and city-level location are all sent live to Gemini Live during an active session; Argus's backend itself never stores camera/mic data; no paid content exists.
    - **Root cause of the triage confusion:** the build Apple actually reviewed was `1.0 (4)` — from mid-July — despite builds 7 through 15 already sitting in TestFlight by the review date. Uploading a build to TestFlight (`eas submit`) does **not** select it for App Store review — that's a separate manual step on the app's **App Store** tab (not the TestFlight tab) in App Store Connect, and it had been left pointed at an old build. **Any future resubmission must explicitly re-select the newest build in that tab** — never assume the latest TestFlight build is automatically what gets reviewed.
    - Bundled into the same fix commit (`473d083`, good practice regardless of whether Apple's specific complaint required it): the known-issue-#22 closed-captions fix, an in-app Privacy Policy link (`mobile/app/(main)/settings.tsx` + `mobile/app/sign-in.tsx`, opens `${BACKEND}/privacy`), a 12s connect-timeout in `home.tsx` so a stalled WebSocket surfaces a clear error instead of an indefinite "Connecting" state, and removal of the "Hi, [name]" header greeting (unrelated user-requested change).
    - Built as build number 16 via `eas build --profile production --platform ios` (autoIncrement correctly bumped past 14/15) and submitted via `eas submit --platform ios --latest` — succeeded. Contrast with an earlier same-day attempt on build 14, which failed with "Build number 14 ... already been used": that binary's baked-in number had already been registered with ASC (from an earlier partial attempt), and the fix was simply a fresh build — no `eas.json` change needed since `autoIncrement: true` was already set from known issue #17.
    - **Not yet done:** select build 16 on App Store Connect's **App Store** tab and hit Submit for Review (see root-cause bullet above — this step was what actually caused the original mismatch). Real-device TestFlight verification of the 2026-07-31/08-01 changes (captions fix, privacy links, connect timeout, purpose strings) is in progress as of this writing.

24. ~~"Delete my data" permanently failed with a misleading "check your connection" error for at least one real user~~ — **root-caused and fixed 2026-08-01; a same-day fix attempt introduced a critical regression that was caught and reverted before shipping further.** Root-caused via live Cloud Run request logs (`gcloud logging read`) plus a direct Firestore REST read (both required a `gcloud auth login` re-auth mid-session) rather than guessing from the generic client error: `DELETE /api/user/:userId` was 403ing repeatedly for one user because their Firestore doc didn't exist at all — confirmed via a 404 on a direct document fetch and its absence from the full `users` collection listing. Cause: `mobile/services/auth.ts`'s `saveUser()` only ever calls the one-time `/claim` endpoint (which mints the device secret used to authorize delete) once, at the moment a brand-new local id is generated, and silently swallows failure (`catch {}`) — a single transient network blip at signup permanently strands that device with an id that has no secret and no automatic retry. Fixed by factoring claim into a reusable `claimSecret()` and retrying it both in `saveUser()` (self-heals on next sign-in) and in `deleteAccount()` (retries right before the destructive call needs it) — safe because retrying `/claim` against a userId with no Firestore doc yet was already allowed by the original guard.
    - **Regression, caught by automated security review before further deploy:** the first fix attempt also loosened `backend/memory.js`'s `claimUserSecret` guard from "refuse if the doc exists" to "refuse only if a `deviceSecret` is already set," reasoning defensively about an unconfirmed edge case (a doc created by some other write, e.g. a `remember_preference` tool call, before claim ever succeeds). This was deployed to Cloud Run. A background security review correctly flagged it as a critical authorization/account-takeover hole: userIds appear in plaintext in Cloud Run request logs (as read during this same diagnosis), so anyone who obtained one could call `/claim` on an existing, data-holding account that simply never got its secret and mint themselves a Bearer token to delete that victim's data. It was also unnecessary — the actual diagnosed account had no doc at all, so the original strict `doc.exists` check never blocked its retry. Reverted `claimUserSecret` to the original guard and redeployed within the same session (`gcloud run deploy argus --source .` from repo root, omitting `--set-env-vars` so it preserves the existing `GEMINI_API_KEY`/`WS_SHARED_SECRET`/etc. from the current revision rather than requiring them again) — confirmed live via `/api/health`. Net effect: the mobile-side retry fix ships as intended; the backend guard is unchanged from its original, safe form.
    - Residual known gap, not fixed: a legacy doc that already has data but no `deviceSecret` (the scenario the reverted change was defending against) has no safe self-heal path today short of a manual/admin Firestore fix — not worth solving speculatively without a confirmed real occurrence, especially given the security cost just demonstrated.
    - Verified via `npx tsc --noEmit` (mobile, clean) and `npm test` (backend, all 4 smoke tests pass) after both the fix and the revert. Mobile side still needs a new EAS build to actually ship (client code doesn't take effect until then); backend side is already live.
    - **Reopened 2026-08-01:** build 17 (which bundles the `claimSecret()` retry fix) was built and submitted, and the user reports "Delete my data" still doesn't work — but this was reported minutes after submission, before Apple's TestFlight processing (5-10 min) would have finished, so it's unconfirmed whether the phone was actually running build 17 yet. Same caution as the captions reopen note directly above: **confirm the actual on-device build number before assuming the code fix is wrong.** If it's confirmed build 17 and still 403s, pull fresh Cloud Run logs for that user's id (`gcloud logging read "resource.type=cloud_run_revision AND resource.labels.service_name=argus" ...`, see this issue's root-causing method above) rather than re-guessing — it may be a *different* user id failing for a *different* reason than the one originally diagnosed.
    - **Real root cause found and fixed, 2026-08-05.** First confirmed production was actually running the safe, reverted `claimUserSecret` (pulled the live image layer straight out of Artifact Registry via the Docker Registry HTTP API — `docker` itself wasn't running, so used `curl` with a `gcloud auth print-access-token` bearer token against `us-central1-docker.pkg.dev/v2/.../manifests|blobs/...` — and diffed `memory.js` against the repo: byte-for-byte match, confirmed live revision `argus-00013-lqp` was deployed ~23s *before* the revert commit's timestamp, i.e. deploy-then-commit, not the vulnerable window it looked like at a glance). With the backend cleared, pulled real Cloud Run request logs (`gcloud logging read`, needed a fresh `gcloud auth login` — the cached token had expired) for `httpRequest.requestMethod=DELETE`: found two build-17 delete attempts, one **success** (`u_90164737...`, clean `POST /claim` → `DELETE`, 40 seconds apart) and one **403** (`u_2a6da2db...`, zero `/claim` requests logged *ever* for that id). Fetched that id's Firestore doc directly (`GET .../documents/users/u_2a6da2db...` via the Firestore REST API) — **404, doc doesn't exist at all**, meaning nothing server-side was blocking a fresh claim; the client simply never asked for one.
      - Actual bug, 100% client-side: `mobile/services/auth.ts` stored the device secret in Keychain (`expo-secure-store`) under one **fixed** key, `"argus_secret"`. iOS Keychain survives app deletion/reinstall; `AsyncStorage` (where `argus_uid` lives) does not. Reinstalling the app — which happened repeatedly across this beta's many TestFlight builds — wipes `argus_uid` and generates a fresh uid on next sign-in, but the *old* uid's secret is still sitting in Keychain under that same fixed key. `saveUser()`'s `if (!(await SecureStore.getItemAsync(SECRET_KEY))) claimSecret(id)` then reads a truthy (but wrong-identity) secret and skips claiming entirely — permanently orphaning the new uid with a secret that will never verify, and `deleteAccount()`'s own retry never fires either since it uses the same "is it null" check.
      - Fixed by scoping the Keychain key per uid (`argus_secret_${id}` via a `secretKey(id)` helper) instead of one fixed key, in both `claimSecret()` and `deleteAccount()`. A fresh or orphaned uid now correctly reads no secret under its own key, so claiming (or `deleteAccount`'s retry) fires normally instead of being masked by a stale value left over from a previous install. No server-side change needed or made — this doesn't touch `claimUserSecret`'s guard at all, so it carries none of the risk the known-issue-#24 regression did. Self-heals the specific already-stuck account (`u_2a6da2db...`) automatically on their next sign-in or next "Delete my data" tap post-update, with no manual Firestore intervention required, since that account's Firestore doc doesn't exist yet and the (unchanged, safe) `claimUserSecret` guard only refuses when a doc already exists.
      - Verified via `npx tsc --noEmit` (clean) and reproducing EAS's bundling step. Built as build 18, submitted to TestFlight, and backend redeployed (`argus-00014-l84`) same session.
      - **Reopened again 2026-08-05, on build 18 this time — but the fix above is confirmed working.** Pulled fresh Cloud Run logs after the user's on-device test: one `POST /claim` (200) followed by **three `DELETE` calls, all 200**, ~5 seconds apart, for the same account — i.e. the 403/orphaned-secret bug is gone; the server-side delete is genuinely succeeding every time. The repeated taps were the tell: user-reported symptom was "tapped Delete, no error, but the app never navigated to sign-in" — consistent with someone re-tapping because nothing appeared to happen, not with a rejected request.
      - **Second, distinct bug found and fixed same session:** `deleteAccount()` in `auth.ts` only wrapped the `fetch` call in `try/catch` — the `AsyncStorage.multiRemove(...)` and `SecureStore.deleteItemAsync(...)` calls immediately after it were unguarded, and `home.tsx`'s `confirmDeleteData` had no `try/catch` around its `onPress` at all. If either of those post-delete cleanup calls threw for any reason, the whole chain became a silent unhandled promise rejection in a release build: no error alert, no navigation to `/sign-in`, nothing — exactly the reported symptom, and exactly consistent with a DELETE that already succeeded server-side three times over. Fixed: `deleteAccount()` now treats the DELETE response as the source of truth and wraps local cleanup in its own `try/catch` (a cleanup hiccup no longer masks an already-successful server-side delete); `confirmDeleteData` now wraps its whole body in `try/catch` (so *any* unexpected throw surfaces the existing "Couldn't delete data" alert instead of vanishing) and tracks a `deletingData` state that disables the header button and shows "Deleting…" while in flight, so a slow response can't be mistaken for nothing happening and re-tapped.
      - Verified via `npx tsc --noEmit` (clean). ~~**Not yet built/submitted or re-tested on-device as of this writing** — needs a new build (19) to ship.~~ **Corrected 2026-08-09:** build 19 (commit `86e03f4`) finished 2026-08-07T23:01Z and carries this fix, as do builds 20–23.
      - **Decision (2026-08-05): deliberately not shipping this in the build being resubmitted for App Store review.** Build 18 already fixes exactly what Apple's 5.1.1/5.1.2 rejection asked for (see known issue #26) and is confirmed working on-device; the user chose to resubmit build 18 as-is rather than wait for a build 19 with this fix. Accepted risk: delete-my-data isn't what Apple flagged this round, but functioning account deletion is covered by a different guideline (5.1.1(v)), so if a reviewer independently tests it during this review cycle and hits the pre-fix silent-failure behavior, that could trigger a separate rejection. **Next session should build 19 (this fix plus whatever else has landed by then) and submit it to TestFlight** — doesn't need to go through App Store review immediately, just needs to exist so the next intentional review submission includes it, and so the fix is actually available to testers in the meantime.

25. **Closed captions never actually had any text to display — root-caused 2026-08-05, unrelated to any of the mobile-side fixes from known issue #22.** Every prior attempt at this bug (the `AsyncStorage` race hypothesis, the `CaptionsContext` refactor) was mobile-side and, on review, was logically correct — `CaptionsProvider` is mounted once at the root above both `Home` and `Settings`, so toggling the switch does immediately update shared React state. The real bug was on the backend the whole time: `backend/server.js`'s Gemini Live session config sets `responseModalities: [Modality.AUDIO]` with no `outputAudioTranscription`. Per the `@google/genai` SDK's own type definitions, an AUDIO-modality-only session never populates `msg.text` (that's TEXT-modality only) unless `outputAudioTranscription: {}` is explicitly requested in the connect config — in which case the transcript instead streams in via `msg.serverContent.outputTranscription.text`, a completely different field the code never read. So `if (msg.text) { ... }` at server.js's `onmessage` handler was checking a field that could never be truthy for this app's actual (voice-only) session type — captions had a fully-correct display pipeline wired up to a source that never emitted anything, on every build back to when captions were first added.
    - Fixed: added `outputAudioTranscription: {}` to the `ai.live.connect` config, and buffer `msg.serverContent.outputTranscription.text` chunks (`transcriptBuffer`, scoped per-connection) as they stream in, flushing the buffered line as a `{type:"text"}` WS message when `msg.serverContent.turnComplete` fires — mirrors how audio itself arrives in chunks, and avoids fragmenting the transcript into one tiny mobile-side line per chunk. No mobile changes needed; `home.tsx`'s existing `"text"` message handling already does the right thing once the backend actually sends one.
    - Verified via `node --check server.js` and `npm test` (all 4 backend smoke tests still pass — this session's suite doesn't exercise a real Gemini round-trip, so it can't catch this class of bug itself). Deployed to Cloud Run same session (`argus-00014-l84`) and **confirmed working on-device against build 18** — user reports captions now show text.

26. **New App Store rejection, build 17 (submitted 2026-08-04): guidelines 5.1.1(i)/5.1.2(i) — app shares data with a third-party AI service without in-app disclosure/consent.** Distinct from the earlier 5.1.1(ii)/2.1 rejection in known issue #23 (that one was about vague camera/mic *permission-prompt* strings; this one is specifically about the third-party-AI-sharing disclosure, which Apple's message explicitly says a Privacy Policy link alone does not satisfy — `sign-in.tsx`'s only disclosure before this fix was a passive "By continuing you agree to our Privacy Policy" footer link). Fixed by adding a dedicated, mandatory consent screen (`mobile/app/consent.tsx`) shown once, before the user can ever reach the camera/mic screen: names Google's Gemini Live API specifically, states what's sent (live camera video, mic audio, relevant stored preferences like allergies) and that it only happens while actively connected, links to the full Privacy Policy, and requires an explicit "I Agree — Continue" tap (a "Don't Agree" option explains the app can't function without it and offers the Privacy Policy instead of forcing acceptance). Wired into routing in both `sign-in.tsx` (after `saveUser()`) and `index.tsx` (for returning users who haven't consented yet) via new `hasAiConsent()`/`setAiConsent()` helpers in `services/auth.ts` (`AsyncStorage` key `argus_ai_data_consent`, cleared on `deleteAccount()` alongside the other identity keys). Also tightened `frontend/privacy.html` to explicitly name Gemini as the third party, state the equal-protection commitment from Google's API terms, and note that in-app consent (not just this page) is where permission is actually requested.
    - Verified via `npx tsc --noEmit` (clean) and reproducing EAS's bundling step (consent route included, bundles cleanly). Built as build 18, submitted to TestFlight, and **confirmed working on-device** — user reports the consent screen appears and functions correctly. Still needs: Resolution Center reply, then re-selecting the build on the **App Store** tab per known issue #23's root-cause note (uploading to TestFlight alone does not select a build for review) before actually resubmitting for review.

27. **Investigated three reported bugs — mid-sentence interruption, slow responses, stalled playback — via real Cloud Run log analysis before patching, 2026-08-07. One fix (server-side VAD tuning) caused a new crash the same day and was reverted.** Root-caused using `gcloud logging read` against the live revision plus `@google/genai@1.43.0`'s own type defs (`genai.d.ts`), not docs/guesswork alone:
    - **Interruption** ("cuts the user off mid-sentence, then responds like a new prompt"): `mobile/app/(main)/home.tsx`'s `startAudioLoop` fully stops, unloads, and reallocates a fresh `Audio.Recording` every `CHUNK_MS` (1s) instead of streaming continuously — real mic dead-air, not just batching delay. That gap was tripping Gemini's default ~800ms `silenceDurationMs` mid-sentence, ending the user's turn early; the next chunk then read as new activity and, per the SDK's default `activityHandling: START_OF_ACTIVITY_INTERRUPTS`, barge-in-interrupted Gemini's own in-progress response (`serverContent.interrupted`, previously never logged or even checked in `server.js`).
    - **Slow responses**: measured real connection-open latency from existing timestamped log lines (`Client connected` → `User:` → `System instruction built` → `Connected to Gemini Live API`) — ~365-425ms warm, ~1s on a cold (scale-from-zero) instance, dominated by the uncached `ipapi.co` geolocation lookup plus cold Firestore/weather warmup. `buildSystemInstruction`'s Firestore+weather parallelization (a suspected bottleneck) was confirmed still genuinely parallel via `Promise.all` in both `agents.js` and `memory.js` — not the cause. **Could not confirm or rule out in-conversation (per-turn) Gemini latency as the dominant driver — no such instrumentation existed before this session** (see fix below).
    - **Stalled playback** ("stops halfway, never finishes"): zero `Gemini error:` / unexplained-close evidence in 3 days / 2,197 log lines at the time — every close in that window was client-initiated. Found `server.js`'s `onclose`/`onerror` discarded the real `CloseEvent`/`ErrorEvent` fields (`.code`/`.reason`/`.message`), so even a real occurrence wouldn't have said why. Also identified a plausible client-side mechanism with zero backend footprint: `home.tsx`'s `playNextInQueue` could get permanently wedged if an iOS audio-session interruption (call, Siri, etc.) unloads the sound without ever firing `didJustFinish`.

    Fixed and still deployed: `server.js` — per-IP geo cache (`geoCache`/`GEO_CACHE_DURATION`, was adding ~150-500ms to *every* connection, not just first load); turn-latency logging (`⏱️ Turn latency: Nms`, time from last forwarded audio chunk to first response chunk — the missing instrumentation from the "slow responses" investigation); `interrupted` logging; `onerror`/`onclose` now log the real reason instead of a static string / `JSON.stringify(err)` (which often serializes to `{}` for these SDK event types). `weather.js` — added a 3s fetch timeout (was the only unbounded network call in the connection-open path). `home.tsx` — `playNextInQueue` gained a duration-scaled watchdog (`Math.max(6000, expectedMs*2+5000)`) plus an explicit `!st.isLoaded` check, so a stuck sound can't wedge the queue forever.

    **Shipped then reverted same day:** added `realtimeInputConfig.automaticActivityDetection: {silenceDurationMs:1500, prefixPaddingMs:300}` to `server.js`'s `ai.live.connect` config as the interruption mitigation. The very first live session on the revision that shipped it (`argus-00015-5xl`) hit a fatal `Gemini session closed 1007 "The audio content type (CONTENT_TYPE_AUDIO) is not supported for this model configuration"` mid-conversation, ~12s after a barge-in interruption — exactly matching a user report of Argus cutting itself off, showing "taking longer than usual," then crashing to the dormant screen. Checked 30 days of logs: this error had **never** occurred before, on any prior revision. Single data point, causation not proven, but reverted the config addition as a precaution (a crash is strictly worse than the interruption it was mitigating) while keeping all the diagnostic logging, which is what surfaced the crash's real cause in the first place — before the `onclose` fix, this would have shown as an unexplained close with zero detail. **Net effect: Bug 1's mobile-side cause (see below) is fixed, but the server-side VAD-tolerance mitigation is not currently deployed.** If interruptions recur, don't blindly re-add that config — first check whether it's specifically this preview model that can't handle explicit `realtimeInputConfig` under barge-in, or something else entirely (n=1 so far).

    Separately root-caused and fixed **"cutting in and out every second"** (reported by the user after the above shipped, distinct from the interruption bug): `startAudioLoop`'s stop→read-file→base64-encode→WS-send sequence was fully blocking the next recording's start — the network+encode round trip was the dominant chunk of the ~1s dead-air window, audible as literal audio stutter. Fixed by making the encode+send fire-and-forget (`getAudioB64(uri).then(...)`) instead of awaited, so the mic restarts immediately after the native stop/unload instead of waiting on the network round trip.

    Commits: `e81f5e2` (initial fixes), `caa141c` (VAD revert + mic dead-air fix + version bump, see #29). **Backend redeployed** to Cloud Run (`argus-00016-xcd`, created ~84s after commit `caa141c` — timing strongly suggests it carries the revert, not independently confirmed via logs since the revert doesn't change any log output). ~~**Mobile fixes (mic dead-air, tool-status badge below) not yet built/submitted** — need a new EAS build.~~ **Corrected 2026-08-09:** this was stale. `eas build:list` shows builds 20/21 (commit `e81f5e2`), 22 (`caa141c`) and 23 (`6347392`) all finished on 2026-08-07/08 — every mobile fix from #27 and #30 has been built and shipped to TestFlight. Don't trust "not yet built" notes in this file without checking `npx eas build:list --platform ios` first; it has now been wrong twice.

    Also added, same session: a small always-visible tool-status indicator in `home.tsx` (bottom-left badge, e.g. "Searching the web") — previously `toolStatus` only rendered inside the captions-gated transcript overlay, so it was invisible whenever captions were off. Reuses the existing `TOOL_LABELS` map; no backend change.

28. **`gcloud run deploy argus --source .` failed with a generic "Build failed; check build logs" — root cause was running it from `mobile/` instead of the repo root, not an app bug.** Pulled the actual Cloud Build log (`gcloud builds log <id>`) rather than guessing: Buildpacks auto-detected `mobile/package.json` (no `Dockerfile` in that directory) and ran `npm ci` under `NODE_ENV=production`, which skips devDependencies; `mobile/package.json`'s `postinstall: patch-package` (added in known issue #21) then failed with `sh: 1: patch-package: not found` since `patch-package` is a devDependency. The backend `Dockerfile` lives at the repo root — `gcloud run deploy --source .` must be run from there, or Buildpacks will silently detect and try to build whatever other Node project happens to be in the current directory instead of failing obviously about the wrong path. No code change; just a reminder for future deploys.

29. **App Store Connect rejected build 19's resubmission: `ITMS-90186` (pre-release train `1.0.0` closed for new submissions) + `ITMS-90062` (`CFBundleShortVersionString` must be higher than the previously-approved `1.0.0`).** Root cause: `mobile/app.json`'s `"version"` had stayed at `"1.0.0"` since build 18 was approved by Apple; `eas.json`'s `autoIncrement:true` (with `appVersionSource:"remote"`) only bumps the *build number* (`CFBundleVersion`), not the marketing version — so every new build kept climbing in build number (18→19) while Apple's already-approved `1.0.0` version train stayed locked to further submissions. Fixed: `app.json` `"version": "1.0.0"` → `"1.0.1"`. **Any future submission needs a version bump here whenever the *previous* version has already been approved by Apple**, independent of and in addition to the build-number autoincrement.

30. **Camera made a continuous "picture taken" shutter-sound noise for the whole duration of a live session — reported by a real tester, 2026-08-07.** Root cause: `home.tsx`'s `startFrameLoop` calls `cameraRef.current.takePictureAsync(...)` every `FRAME_MS` (2s) to feed frames to Gemini — a genuine still-photo capture via `expo-camera`, not a video-frame sample. `expo-camera@17.0.7`'s `CameraPictureOptions.shutterSound` defaults to `true` (confirmed in `Camera.types.d.ts`), and the call never set it — so every single frame capture played the OS shutter sound, continuously, for as long as the session stayed connected. This was true from when frame capture first shipped (known issue #4) and had gone unreported until now. Fixed by passing `shutterSound: false`. **Caveat, not fully resolved everywhere:** per Apple's own restrictions, iOS's shutter sound is normally tied to the device's ringer/silent switch, but in Japan and South Korea specifically it's hardware-locked to always play regardless of what any app requests — this fix should resolve it for the vast majority of devices/regions but may not eliminate it entirely in those locales. Commit `43ea995`. ~~**Not yet built/submitted to TestFlight**~~ — **corrected 2026-08-09:** shipped in build 23 (commit `6347392`), finished 2026-08-08T00:48Z, alongside #27's mobile fixes and #29's version bump.

31. ~~Every audio+video Live session was hard-capped at 2 minutes and silently terminated~~ — **found and fixed 2026-08-09, during a 200-concurrent-user production-readiness audit.** Root-caused from Google's own Live API docs, not guesswork: a session streaming both audio and video is limited to 2 minutes by default and terminates with **no error at all** once that's hit — `server.js`'s `ai.live.connect` config had neither `contextWindowCompression` nor `sessionResumption` set, and Argus streams mic audio + camera frames in the same session, so this applied to every real conversation, not an edge case. This is a better explanation than the iOS-audio-interruption theory in known issue #27's "stalled playback" investigation: a session-limit termination produces a plain `onclose`, not a `Gemini error:`, and #27's own logs showed zero error evidence across 3 days/2,197 lines — consistent with a clean 2-minute cutoff, not a crash. Fixed by adding `contextWindowCompression: { slidingWindow: {} }` to the connect config (cheaper than the other documented option, session resumption, which would've needed reconnect-with-handle logic added to both `frontend/index.html` and `mobile/home.tsx`). Verified via `node --check`, `npm test` (4/4 smoke tests), and a live WS connection against the deployed revision. Deployed same session (`argus-00017-fjv`). **Verification caveat, added 2026-08-09 (see #33):** that "live WS connection" was the auth-gate rejection test, *not* a real Gemini round trip. Cloud Run logs confirm `argus-00017-fjv` served **zero** real conversations across its entire lifetime (20 log lines, all deploy/startup) before being superseded by `argus-00018-mhv` — so `contextWindowCompression` had never actually been exercised by a live session. Don't read "verified against the deployed revision" as "a real session ran."

32. **200-concurrent-user production-readiness audit, 2026-08-09 — Cloud Run had no real capacity margin, no fleet-wide spend cap, and the geo lookup couldn't clear 200 users without throttling.** Scope was explicitly the harder interpretation (200 simultaneous live audio+video sessions, not 200 registered accounts). Full layer-by-layer verdict below; all backend changes verified via `node --check`, `npm test` (4/4 passing throughout), and a live WS auth-gate test against the deployed revision — confirms the deployed code, not just a locally-tested version, is what's actually running (see [[argus-cloudrun-stale-deploy]] for why that distinction matters here specifically). Deployed same session to `argus-00017-fjv`; live config confirmed post-deploy via `gcloud run services describe`: `containerConcurrency=40`, `maxScale=15`, `memory=1Gi`.
    - **Gemini Live API quota**: confirmed project is on paid Tier 1 (billing linked) — Free tier's 10 RPM/250 RPD for plain Flash wouldn't have cleared even a handful of concurrent sessions. Google doesn't publish a fixed concurrent-session ceiling for Live specifically — their own staff say it's governed by tokens-per-minute (TPM) for the project's tier, not a session count — so there's no single number to point at going forward. Tier 1 is a real improvement over Free but has not been load-tested against a genuine 200-concurrent burst (see "not done this session" below).
    - **Cloud Run capacity**: a local relay-only load test (real `server.js`/`agents.js`/`memory.js`, Gemini mocked out so it cost nothing) measured ~0.7-0.9MB additional RSS per connection at 250 simulated concurrent WS clients streaming realistic audio/image cadence. On the then-live `512Mi`/implicit-concurrency-80 config, relay overhead *alone* — excluding the real `@google/genai` SDK's own per-connection upstream-session overhead, which the mock can't capture — was already eating over half the container's memory budget at that connection count. Fixed: `--memory 512Mi` → `1Gi`, implicit concurrency 80 → explicit `--concurrency 40`, `--max-instances 10` → `15` (600 total slots vs. the 200 target, with margin for uneven load distribution since session affinity sticks reconnects to one instance).
    - **No fleet-wide spend cap**: `connectionsByIp` in `server.js` is per-instance memory only, so `WS_MAX_CONN_PER_IP` was never a real total-spend bound, and nothing else capped total concurrent sessions across the fleet. Added a Firestore sharded counter (`reserveGlobalSlot`/`releaseGlobalSlot` in `memory.js`, 10 shards to stay under Firestore's ~1-write/sec-per-document contention limit under bursty connect/disconnect) enforcing `MAX_GLOBAL_CONCURRENT_SESSIONS` (env var, default 250) — a connection past budget now gets a clean `4029` close instead of the fleet silently growing an uncapped bill. Fails open if Firestore is unavailable (per-IP/per-instance limits remain as backstop) rather than blocking every connection on a memory-backend outage.
    - **ipapi.co would throttle well before 200 users** (free tier: 1,000 req/day, called once per uncached connecting IP). Swapped to `ip-api.com` (45 req/min ≈ 64,800/day, no signup) — **a background security review then correctly flagged that this introduced a plaintext-HTTP MITM/prompt-injection path**, since `ip-api.com`'s free tier has no HTTPS and the returned city/region strings flow straight into the Gemini system prompt (`userCity` → `buildSystemInstruction`). Checked the suggested HTTPS alternative, `ipwho.is`, first — it shares the same ~1,000/day-per-caller ceiling as the original `ipapi.co`, so it wouldn't have actually fixed the scaling problem this swap existed to solve. Fixed instead with `sanitizeLocationField()` in `server.js`: strips everything except letters/digits/spaces/`,.'-`, caps to 100 chars, before the geo response reaches the prompt — a MITM-forged response can no longer inject newlines or instruction-shaped structure, only an inert wrong-looking location string. Also added strict `Number.isFinite` + range validation on the returned lat/lon before they reach `weather.js`'s URL (previously any truthy value passed through unvalidated). Residual, accepted risk, documented in the code comment: the transport itself is still plaintext — no free-tier HTTPS geo API exists with an adequate rate ceiling.
    - **Billing budget alert**: there was no spend cap anywhere in the stack — Gemini Live billing scales directly with concurrent open connections, compounding known issue #12 (WS secret readable in public HTML). Created a live GCP billing budget (billing account `01B234-07EDB3-224CBF`) at $500/month with alert emails at 50/90/100/150% thresholds. Also added the equivalent `google_billing_budget` Terraform resource (optional, `count`-gated on a `billing_account_id` variable) for parity, though `deploy-cloudrun.sh` remains the actual deploy path this repo uses, not Terraform.
    - **Firestore**: confirmed billing-enabled (`gcloud billing projects describe` — Blaze-equivalent), and estimated real volume at 200-user scale (~13,000 ops/day under a generous usage assumption) stays comfortably under Firestore's always-free 50k reads/20k writes/20k deletes-per-day quota. No fix needed.
    - **Mobile client**: `ArgusSocket` has no auto-reconnect/retry logic (connect is user-initiated, tap-to-awaken), so there's no thundering-herd/retry-storm risk under a degraded or capacity-limited backend — worst case is a manual-retry error toast. No fix needed.
    - **Not done this session, deliberately**: no real (non-mocked) Gemini load test — at 10-20 connections or at 200 — was run. The local test was relay-only by design (zero Gemini cost, per explicit choice at the time) and is a lower bound, not a real capacity measurement. If 200-concurrent capacity needs to be validated before it matters in production, that real test — plus a real capacity request to Google, since there's no published concurrent-session/TPM figure to plan against — is the next step, not something to assume from this session's numbers alone.
    - **Open action item**: an early `gcloud run services describe` call during this session printed the live `GEMINI_API_KEY` in plaintext into the chat transcript (`WS_SHARED_SECRET` came back redacted; the Gemini key didn't). Flagged to the user in-session. Rotating that key (new one in AI Studio, update on the live Cloud Run service, revoke the old one) is still outstanding — see action items below.

33. **Four reported voice symptoms — slow responses, cutting out mid-turn, Argus talking over itself, and the app "going out of viewing mode" unprompted — root-caused to three client-side defects, 2026-08-09.** Diagnosed from real Cloud Run logs (5-day window, 21 sessions) and the deployed image, before any code was written. Commit `c696bca`; backend deployed as `argus-00018-mhv`; mobile shipped as build 24.
    - **Step 0 first.** Confirmed `argus-00017-fjv` was live, then pulled `server.js` out of the deployed image layer via the Artifact Registry HTTP API and hashed it — byte-identical to `main`. No stale-deploy gap this round (contrast [[argus-cloudrun-stale-deploy]]). But logs showed that revision had served **zero real conversations** in its entire lifetime, so every reported symptom predates it and belongs to `argus-00016-xcd` or earlier. Also found this file's "not yet built" claims for #24/#27/#30 were all wrong — builds 19–23 existed (corrected inline above).
    - **Headline finding — the microphone was silently dead in ~30% of sessions.** `server.js` logs `🎤 Audio chunks: N` at `N % 100 === 1`, so the absence of that line proves zero chunks arrived. Six of 21 sessions streamed camera frames normally while sending **no mic audio at all**, including 103s, 60s and 51s sessions. One example: 24 frames at a perfect 2s cadence over 51 seconds, not one audio chunk. Worsening by revision: `argus-00014` 1/5, `argus-00015` 1/8, `argus-00016` **4/4**.
    - **Bug A — dead mic (explains "slow responses").** `startAudioLoop()` was called from a `setTimeout` with **no `.catch()`**, and set `loopRef.current = true` *after* `await Audio.setAudioModeAsync(...)`. Any rejection there became an unobserved promise: the mic loop never started, `startFrameLoop()` (a separate synchronous call) carried on, and nothing surfaced to the user or the logs. "Slow" was never latency — every logged turn latency is 41–1037ms, median ~350ms; Gemini answers promptly whenever it actually receives audio. The dead-mic sessions are the *long* ones precisely because the user kept waiting. Fixed: `loopRef` set before the fallible call, 3× retry with backoff, a real `.catch()` surfacing "Microphone unavailable", and an awaited mic-permission check at connect (it had been fired on mount with the result discarded). **The exact reject trigger is still unconfirmed** — leading hypothesis is contention with the greet reply's `Audio.Sound.createAsync` (greet fires at +1200ms, `setAudioModeAsync` at +1500ms). The new `console.warn` will name it next time.
    - **Bug B — stale sockets (explains both "cutting out mid-turn" and "goes out of viewing mode").** `ArgusSocket.disconnect()` did `this.ws?.close(); this.ws = null;` — but `close()` is async and nulling a reference **does not detach** `onmessage`/`onclose`. The abandoned socket kept firing into the shared `handleMsg`, whose `disconnected` branch runs `setStatus("dormant")`; since `connected` gates `<CameraView>`, the camera unmounted mid-conversation with no user action. **This was never a crash** — the user explicitly corrected that mid-session, which is what identified it as the same root cause rather than a separate bug. Fixed: handlers explicitly detached, greet timer tracked/cleared, `disconnect()` idempotent, and every message stamped with an `epoch` that `handleMsg` checks first.
    - **Bug C — overlapping audio ("talking over itself").** Two paths, both confirmed by code reading. (1) `caa141c` changed the audio send to capture `sock = socketRef.current` before the encode, so a chunk could be delivered into an older still-open Gemini session whose reply then played over the live one — this is why the symptom got *worse*. (2) `playNextInQueue` created sounds with `shouldPlay: true`; a `stopPlayback()` landing during that `await` left an untracked already-playing sound while `isPlayingRef` was reset to false, letting the next burst start a second sound on top of it, neither stoppable. Fixed: send re-reads the current socket, and playback creates paused then checks a `playbackTokenRef` before `playAsync()`.
    - **Backend**: added `sessionsByUser` — closes any prior session for a `userId` (code `4002`) before opening a new one. Nothing previously prevented one user holding several concurrent Gemini sessions. **No log instance of this was ever observed** (the only overlapping sessions in the window were two different users on different IPs); the capability is confirmed by code reading only, and the new `♻️ Superseding prior session` line will settle it on the next real session.
    - **Removed an 81% log flood**: `if (msg.text)` fired the SDK's `.text` getter on every audio chunk. The session is AUDIO-only so it can never return anything useful, but touching it concatenates all parts and emits a `there are non-text parts inlineData...` warning — **3,232 of 4,000 log lines**, plus per-chunk string work on the hot relay path. It was drowning out every real diagnostic. Captions already come from `outputTranscription`.
    - **Still open**: `mobile/` has **no crash reporting at all** (no Sentry/Crashlytics/Bugsnag). It didn't block this session once the "crash" was reclassified as Bug B, but any future genuine crash is undiagnosable. Also unfixed: `staysActiveInBackground` is false and there's no WS keepalive/ping, so a backgrounded phone or an idle NAT timeout can still drop a session — now at least visible as a "Connection dropped" toast rather than a silent revert to dormant.
    - Verified: `tsc --noEmit` clean, `node --check` OK, `npm test` 4/4, EAS bundling step reproduced locally (985 modules), deployed image re-hashed against the repo post-deploy (match), all 7 env vars preserved. **Not device-verified** — that needs build 24 on a real phone.

34. **The live `WS_SHARED_SECRET` is a documentation placeholder, not a secret — found 2026-08-09 while verifying the key rotation.** The value deployed on Cloud Run is the literal 42-character string `<same value as backend's WS_SHARED_SECRET>` (confirmed by fetching `GET /`, which injects it into the page per `serveIndexWithSecret`, and checking its shape: 42 chars, not 64-hex, matches `^<.*>$`). It was almost certainly pasted from a setup instruction during an early deploy and never replaced. Consequences:
    - The WS auth gate added in known issue #9 is guarded by a **guessable constant derived from this repo's own docs** — strictly worse than known issue #12's framing, which assumed the secret was real but readable by anyone loading the public page. It doesn't require visiting the page at all.
    - Gemini Live billing scales with concurrent open connections. The only remaining bounds are `MAX_GLOBAL_CONCURRENT_SESSIONS` (250) and the $500/month budget **alert** (an alert, not a hard cap).
    - Both clients must be changed together or they break: the backend env var, `EXPO_PUBLIC_WS_SHARED_SECRET` in EAS (`eas env:update production ...`, then a new build, since it's baked in at build time), and any local `mobile/.env`. The web frontend needs no change — it reads whatever the backend injects.
    - **Not fixed in this session** — rotating it mid-session would have instantly broken every installed TestFlight build (including build 24, submitted the same day) until a new build shipped and testers updated. Sequence it deliberately: generate a real secret (`node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`), set it on Cloud Run and in EAS, cut a build, and only then confirm old builds are expected to fail.

35. **Build 24 on-device test: the mic fix is confirmed working; one ~36s mid-session pause remains unexplained — 2026-08-09.** First real-device session on the #33 fixes (23:25:02–23:26:57 UTC, revision `argus-00019-zcz`).
    - **Confirmed fixed:** `Audio chunks: 1` at 23:25:04 **and `Audio chunks: 101` at 23:26:50** — 100 chunks in 105.6s, the exact 1/sec cadence. This is the first session on record to pass chunk 100 (the counter logs at `N % 100 === 1`, and every prior session topped out at the single `1` line). Mic streamed continuously for the whole session, **including through the pause**. Also clean: zero `Gemini error`, zero `Superseding prior session` (no duplicate sessions), 42 frames at an unbroken 2s cadence, client-initiated `1000` close, and the SDK log flood at **0** (166 lines/3h vs. thousands).
    - **The pause: 23:25:46 → 23:26:22, 36 seconds.** Ruled out by evidence — not the mic (audio flowed throughout), not the network (frames unbroken), not the tool (`get_restaurant_website` returned in 138ms, falling back to a Maps link). **Root cause not determined**, because the logging could not distinguish "Argus talked for 36s" from "Argus was silent for 36s": outgoing audio to the client was never counted, and the turn-latency metric is structurally incapable of showing it (see below).
    - **Instrumentation flaw found, and an earlier claim in #33 corrected.** `lastAudioForwardedAt` updates on *every* inbound audio chunk including silence, and the client sends one per second regardless of whether anyone is speaking — so `⏱️ Turn latency` can never exceed ~1s and is really "time since the last 1s chunk," not response delay. The 1038ms reading at the pause was the metric saturating. #33's "every logged turn latency is fast (median ~350ms), Gemini answers promptly" was therefore weaker evidence than stated; that conclusion still holds, but it rests on dead-mic sessions being the long ones, **not** on those latency figures. Don't cite them as response-time evidence.
    - Fixed by adding: `🗣️ Response turn started — Nms quiet since last turn ended`, `🔇 Turn complete — N audio chunks over Nms`, and `🎙️ User speech detected` (via a new `inputAudioTranscription: {}`, a sibling of the already-working `outputAudioTranscription`). Next occurrence self-diagnoses: a 36s *turn* means Argus over-talking; a 36s *quiet gap* full of user-speech events means Gemini heard speech and didn't respond, which points back at the VAD tuning reverted in #27. Transcript **text** is logged only under `LOG_TRANSCRIPT_TEXT=1` — the App Store disclosure says Argus doesn't store speech, and Cloud Run logs are retained, so leave it off unless actively debugging. **Watch the first sessions on this revision**: the last config added near this area caused the `1007` crash in #27.

36. **200-concurrent-user capacity: infrastructure resized and now plausibly sufficient; the Gemini-side ceiling remains genuinely unknown — 2026-08-09.** Re-examined against the live config rather than #32's notes. Deployed as `argus-00020-7dt`.
    - **Real blockers found that #32 missed, all in the "many users, not many sessions" direction:**
      - `WS_MAX_CONN_PER_IP` was **5**. Mobile carriers put large numbers of subscribers behind a handful of CGNAT addresses (campus/office WiFi likewise), so at real user counts this rejects legitimate users who merely share an egress IP. It is an anti-abuse guard, not the spend cap. Now env-configurable, default **50**.
      - `/api` per-IP HTTP limit was **60/min**. Every install calls `POST /api/user/:id/claim` exactly once, and a *failed* claim is precisely what stranded a device with no device secret in known issue #24. Now **300/min**.
      - `MAX_GLOBAL_CONCURRENT_SESSIONS` default **250 → 400** — the sharded counter is deliberately approximate, so a cap only 25% above the target would reject real users before 200 was reached.
    - **Cloud Run resized `1cpu/1Gi/concurrency-40/max-15/min-0` → `2cpu/2Gi/concurrency-25/max-20/min-1`** (500 slots vs. the 200 target). **The binding constraint is CPU, not memory** — #32 sized on memory because its load test measured ~0.7–0.9MB RSS/connection, but each session JSON-parses a ~43KB base64 audio chunk every second plus a ~150KB base64 JPEG every 2s and re-serialises both to the Gemini SDK. 40 such streams on 1 vCPU was tight while looking comfortable on memory. `min-instances 1` removes the cold start logs showed on essentially every session. Same sizing mirrored into `terraform/main.tf` so the two deploy paths don't drift.
    - **Already fine, verified not assumed:** `timeoutSeconds` is **3600**, so WebSockets are not cut by Cloud Run's 300s default (this would otherwise have killed every session at 5 minutes). Firestore sharded counter math checks out — 200 users on ~2min sessions is ~3.3 writes/sec across 10 shards (0.33/shard/sec), well under the ~1 write/sec/document contention limit; no change needed.
    - **Still unverified, and not resolvable from here: the Gemini Live concurrent-session ceiling.** The Service Usage API exposes exactly one bidi/Live quota for this project — `Bidi generate content active sessions (paid tier 3)` at `effectiveLimit: -1` — and publishes **no** tier-1 or tier-2 bidi limit, so the real ceiling cannot be read from the API. Combined with #32's finding that Google's own staff describe Live as TPM-governed rather than session-capped, there is still no number to plan against. **No real (non-mocked) Gemini load test has ever been run** — #32 deliberately skipped it and this session did not change that. If 200 concurrent matters before it happens in production, that test plus a capacity request to Google is the next step; the infrastructure is now sized for it, which is not the same as proven.

## Session status — iPhone testing + App Store readiness plan (started 2026-07-11, resumed after a mid-session shutdown)

Working through a 6-phase plan: (1) core mobile functionality, (2) security, (3) performance/reliability, (4) automated smoke checks, (5) iOS-specific readiness, (6) App Store readiness report. Phases 1–5 are done as of 2026-07-12; phase 6 is a standalone report (see below, not stored in this file).

**Phase 1 — Core functionality: confirmed done.** `mobile/app/(main)/home.tsx` genuinely captures camera frames and plays back audio (see known issue #4).

**Phase 2 — Security: done**, see known issues #8–9 above for what changed. Origin-header checks were considered and deliberately **not** added — the realistic threat here (scripted clients hitting the URL directly) doesn't send an `Origin` header at all, so the shared secret already covers it more effectively than an origin allow-list would. **Revisit:** known issue #12 (secret is readable in the public HTML) means this phase's protection is weaker against a deliberate/motivated actor than the original writeup implied — read it before treating WS auth as a hard boundary.

**Phase 3 — Performance/reliability: done.**
- `backend/agents.js`'s `timers` Map was module-level, shared across all connections (same bug class as the already-fixed `currentUserId` global) — fixed by scoping it per-`userId` via `getUserTimers(userId)`.
- `server.js`'s Gemini `onerror` callback notified the client but never closed the socket or cleared `session`, unlike `onclose` — a Gemini-side error not followed by a close event left a zombie connection holding a per-IP connection slot indefinitely. Fixed: `onerror` now closes the client socket too, which routes through the existing `clientWs` `close` handler cleanup.
- `memory.js` read/write volume per tool call: reviewed, no fix needed — each tool call does 0–2 sequential Firestore round trips (~50–150ms each), a minor voice-latency add but not a real bottleneck at personal-testing scale.
- Worst-case Gemini cost: rate limits (5 conns/IP, 30 msg/sec/conn) bound cost *per IP*, but there's no global concurrency cap, and known issue #12 means the secret doesn't actually restrict *who* can open connections — so total worst-case cost is effectively unbounded if the public URL gets traffic. Rough per-connection order of magnitude at `gemini-2.5-flash-native-audio` pricing ($3/1M audio-input tokens, $12/1M audio-output tokens, $3/1M image tokens, ~25 tokens/sec of audio): well under $0.10/minute per open connection: continuous audio in+out plus a frame every ~2s.

**Phase 4 — Automated smoke checks: done.** Added `backend/test/server.smoke.test.js` (Node's built-in `node:test`, no new dependency) — see Commands section above for what it covers and its deliberate scope limits. No lint/typecheck added: mobile's existing `npx tsc --noEmit` already covers typechecking there, and backend genuinely has nothing today; left as optional/low-priority since a lint config is a style decision, not a correctness gap.

**Phase 5 — iOS-specific readiness: done, with one open item.**
- `NSCameraUsageDescription`/`NSMicrophoneUsageDescription` already present in `mobile/app.json`.
- known issue #11 (unused Android `READ_SMS`/`RECEIVE_SMS`) fixed.
- Expo Go vs EAS build: **Expo Go works as-is** for dev iteration — every native dependency (`expo-camera`, `expo-av`, `expo-secure-store`, `react-native-reanimated`/`worklets`, `expo-router`, etc.) is within Expo Go SDK 54's bundled module set, no custom native module requiring a dev client. Caveat: Expo Go uses its *own* bundled permission strings/manifest, so the custom `NSCameraUsageDescription`/Android `permissions` in `app.json` only take effect in a real EAS build — camera/mic *function* identically in Expo Go, but you won't see Argus's actual permission prompt copy until testing an EAS build. `mobile/eas.json` already has a `development` profile (`developmentClient: true`, internal distribution) ready to use.
  - Quick dev-loop, no Apple Developer account needed: `cd mobile && npx expo start`, scan the QR with the Expo Go app.
  - Real dev-client build (needed to see final permission copy, and eventually to submit): `cd mobile && npx eas login && npx eas build --profile development --platform ios`, install on device, then `npx expo start --dev-client`.
  - Production/App Store build: `npx eas build --profile production --platform ios && npx eas submit --platform ios`.

**Phase 6 (App Store readiness report):** report-only, delivered directly to the user at the end of the 2026-07-12 session rather than stored here (it doesn't hold up as durable repo guidance — Apple Developer account status and App Store Connect listing state are facts about the user's Apple account, not the code).

**2026-07-13 — first production build + first physical-device test:**
- Got a production EAS build all the way to a signed `.ipa` after fixing three build-time issues in sequence (known issues #13, #14 above — an npm peer-dep conflict, then a missing `babel-preset-expo` devDependency), each one verified by reproducing EAS's exact failing command locally before pushing again, rather than push-and-pray.
- `eas submit --platform ios --latest` succeeded; build reached "Ready to Submit" in App Store Connect's TestFlight tab (this status just means Apple's processing finished, not that the App Store listing is complete).
- **First real device test failed**: installed via TestFlight on a physical iPhone, app crashed to expo-router's "Unmatched Route" screen. See known issue #15 for the working hypothesis and exact next steps — **this blocks everything else** (App Store Connect listing work, further Phase 6 steps, and the still-unverified audio-session interaction from known issue #4) until the app actually launches on-device.
- Stopped here for the night per user request. **Next session starts at known issue #15**, not at a new phase.

**2026-07-12, later same day — iOS store prep:**
- Decision on known issue #12: user explicitly wants the web frontend (`frontend/index.html`) to stay low-traffic/unpromoted and the iOS app to be the surface that gets real traffic. Given that, the web page's secret-in-HTML exposure is an accepted risk as long as the page isn't linked/promoted — not fixed further, and still worth revisiting if that assumption ever changes.
- Added `ITSAppUsesNonExemptEncryption: false` to `mobile/app.json`'s `ios.infoPlist` — the app only uses standard TLS (HTTPS/WSS) and `expo-crypto`'s `randomUUID()` (not custom encryption), which qualifies for Apple's standard export-compliance exemption. Avoids a manual App Store Connect encryption-compliance question on every build/submission.
- **Critical gap found for production builds:** `mobile/services/websocket.ts` throws at startup if `EXPO_PUBLIC_BACKEND_URL` is unset, and silently sends an empty `secret` if `EXPO_PUBLIC_WS_SHARED_SECRET` is unset (every WS connection then gets rejected with 4001, with no clear error surfaced to the user). Both are read from `mobile/.env` for local dev, but **EAS cloud builds don't have access to that local file** — nothing in `eas.json` declares `env` per build profile, and neither var has been pushed to EAS's own environment-variable store. Without setting them there first, a `production`/`preview` EAS build will either crash on launch or silently fail to connect. Fix is a manual `eas env:create` step (needs the user's EAS login) — not something to commit into `eas.json` for the secret value, since that would recreate the exact plaintext-secret-in-a-committed-file problem from known issue #9/#12.

**2026-07-16 — crash fix session:**
- Fixed known issue #15 (the "Unmatched Route" crash) and known issue #16 (`remember_preference` Firestore bug) — see those entries above for root cause and fix detail. Both verified via `tsc --noEmit`, a local reproduction of EAS's exact bundling step, `node --check`/`npm test` on the backend, and an independent second-pass review that re-derived the root causes from source rather than trusting the first pass.
- While investigating, checked EAS's own environment-variable store (`npx eas env:list --environment production`, already logged in as `rollie2347` in this environment) as part of chasing the crash: `EXPO_PUBLIC_BACKEND_URL` in the `production` environment was literally set to the placeholder string `https://<your-cloud-run-url>`, not a real Cloud Run URL — `preview`/`development` have no variables set at all. This doesn't cause a launch crash (the var is non-empty, so `services/websocket.ts`'s module-level throw doesn't fire), but means a `production`-profile build launches fine and then fails to ever connect. Initially deferred at the user's call; **fixed later same session** once it actually blocked testing — see known issue #17.
- App rebuilt and resubmitted after the #15 fix; **first physical-device relaunch succeeded, no more crash.** Follow-up round of real-device testing then surfaced three more issues, all fixed same session — see known issues #17 (backend URL + build-number collision), #18 (audio playback jitter), #19 (mostly-empty home screen).

**2026-07-28 — TestFlight-tester UI feedback round:** with a build sitting in Apple App Store review, the user relayed real tester feedback about the mobile app's UX: confusing mute button, silent long waits, stacking connection errors, emoji-heavy tool-status text, and quiet playback. Fixed all five at the code level — see known issue #20 for the detail. Iterated once more on the mute control mid-session: an initial version flashed a "Muted"/"Unmuted" label on toggle, which the user asked to drop in favor of just the plain on/off slider. Confirmed submitting a new TestFlight build via `eas submit` is independent of the build currently under App Store review (that's a separate manual "pick a build and submit for review" step in App Store Connect), so shipping this to testers now doesn't disturb the pending review. **Not yet built or resubmitted** — do that next, then verify on a real device before closing out issue #20, especially the volume mitigation (a digital gain boost, not a real fix for the underlying earpiece-routing cause).

**2026-07-31 to 2026-08-01 — first real App Store review cycle:** the app was submitted for App Store review and rejected under guidelines 2.1 and 5.1.1 — see known issue #23 for the full detail, root causes, and fixes (closed-captions Context refactor, in-app privacy links, connect timeout, rewritten camera/mic purpose strings). Build 16 has been built and submitted to TestFlight. **Next session (or later this one) should start by checking whether the user has re-selected build 16 on App Store Connect's App Store tab and resubmitted for review**, and whether TestFlight testing on-device confirmed the closed-captions fix — do not assume either happened just because the code shipped.

**Your action items (nobody else can do these):**
1. Generate a shared secret: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
2. Set `WS_SHARED_SECRET=<that value>` in your real `backend/.env` (local dev) and redeploy Cloud Run with it — `./deploy-cloudrun.sh <GEMINI_API_KEY> <WS_SHARED_SECRET>` (argument order changed, secret is now positional arg 2) or the Terraform equivalent with `-var="ws_shared_secret=..."`.
3. Set the **same** value as `EXPO_PUBLIC_WS_SHARED_SECRET` in `mobile/.env`, plus `EXPO_PUBLIC_BACKEND_URL` (now required — the code no longer falls back to a hardcoded URL).
4. Decide how to handle known issue #12 (secret readable in public HTML) — accept the risk at current scale, or ask for a redesign (e.g. per-session token minted server-side instead of a static secret baked into the page).
5. A physical iPhone and an Apple Developer account, needed for the EAS development build step in Phase 5 above.
6. Optional/low-priority: `mobile/.env.example` has what look like real Google OAuth client IDs rather than placeholders — not a secret (OAuth client IDs are meant to be public), just unusual for an example file. Your call whether to genericize it.
7. ~~**Rotate `GEMINI_API_KEY`.**~~ **Done 2026-08-09.** The exposed key (`9085c1b3…`, created 2026-03-01) has been replaced and deleted. Rotation was done entirely through `gcloud`, not AI Studio — AI Studio keys for a GCP project are ordinary Cloud API keys, so `gcloud services api-keys create --api-target=service=generativelanguage.googleapis.com` mints a like-for-like replacement, `... get-key-string` retrieves it, and `... delete` revokes the old one. New key: uid `8365ecf8-0b2a-4d9d-83d0-28155b0d67b9`, display name "Argus (rotated 2026-08-09)", same single-API restriction. Deployed as `argus-00019-zcz` and verified **before** revoking the old key by opening a real Gemini Live session against the live service (`/api/health` is useless for this — it returns static config and passes with a dead key; the only real proof is the server emitting `{type:"connected"}`, which it only does from the Gemini session's own `onopen`). Re-verified after revocation. Note `backend/.env` does not exist on the current dev machine, so no local update was needed — recreate it with the new key if local dev is ever resumed.
    - **Two transcript-leak traps hit while doing this**, worth knowing for any future secret handling: (a) `gcloud services api-keys create` prints the full new key in its operation result, and `*> $null` does **not** suppress it reliably in PowerShell 5.1 (stderr redirection of a native command raises `NativeCommandError`, which aborts the script under `$ErrorActionPreference="Stop"` while still surfacing output). Retrieve the uid by `--filter`/`--format` and fetch the string separately into a variable instead. (b) `gcloud services api-keys delete` echoes the **entire key object, including `keyString`**, into the transcript — harmless for a key being revoked in that same command, but do not run it against a key you intend to keep.
8. **Rotate `WS_SHARED_SECRET` — it was never actually set (see known issue #34).** Action items 1–3 above were written when the WS auth gate was added and were evidently followed with the *placeholder text* rather than a generated value, so the live secret is `<same value as backend's WS_SHARED_SECRET>`. Redo them for real: generate with `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`, set on Cloud Run (`gcloud run services update argus --region us-central1 --update-env-vars WS_SHARED_SECRET=...`) **and** in EAS (`eas env:update production --variable-name EXPO_PUBLIC_WS_SHARED_SECRET --value ...`), then cut a new build. **Every already-installed TestFlight build stops connecting the instant this changes** — including build 24 — so do it alongside a build you're ready to ship, not in isolation.

## Onboarding order for a fresh session

Read `README.md` → `ARCHITECTURE.md` → `backend/server.js` → `backend/agents.js` → `mobile/app/(main)/home.tsx`. Run the backend and validate the web PWA (`frontend/index.html`) end-to-end before touching the Expo app — it's the most complete surface and the fastest way to confirm the Gemini Live pipeline still works.
